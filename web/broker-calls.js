(()=>{
const API2=window.API||'https://stockpulse-india-api-v3.onrender.com/api';
async function loadBrokerCalls(){const sec=document.getElementById('broker-calls');if(!sec)return;sec.innerHTML='<div class="panel"><h2>Broker & Analyst Calls</h2><p class="muted">Public broker/analyst recommendations are shown separately from StockPulse scoring. No calls are fabricated when a public feed is unavailable.</p><div id="broker-calls-body">Loading…</div></div>';try{const r=await fetch(API2+'/broker-recommendations',{cache:'no-store'});const d=await r.json();const rows=d.items||[];document.getElementById('broker-calls-body').innerHTML=rows.length?`<div class="tablewrap"><table class="table"><thead><tr><th>Stock</th><th>Broker / Analyst</th><th>Rating</th><th>Target</th><th>Upside</th><th>Horizon</th><th>Date</th><th>Reason</th></tr></thead><tbody>${rows.map(x=>`<tr><td><b>${x.symbol||'—'}</b></td><td>${x.broker||x.source||'—'}${x.analyst?' / '+x.analyst:''}</td><td>${x.rating||'—'}</td><td>${x.target_price??'—'}</td><td>${x.upside_pct??'—'}</td><td>${x.horizon||'—'}</td><td>${x.date||'—'}</td><td>${x.rationale||'—'}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">No public broker/analyst calls are currently available from the connected feed.</div>'}catch(e){document.getElementById('broker-calls-body').innerHTML='<div class="empty">Broker feed unavailable right now.</div>'}}
function init(){if(document.getElementById('broker-calls')){loadBrokerCalls();return}const main=document.querySelector('main');if(!main)return;const s=document.createElement('section');s.id='broker-calls';s.className='section';main.appendChild(s);loadBrokerCalls()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();
