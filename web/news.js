(function(){
const API='https://stockpulse-india-api-v3.onrender.com/api';
const esc=s=>String(s??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m]));
const n=v=>Number(v)||0, money=v=>n(v).toLocaleString('en-IN',{maximumFractionDigits:2}), pct=v=>(n(v)>=0?'+':'')+n(v).toFixed(2)+'%';
const getWatch=()=>{try{return JSON.parse(localStorage.getItem('stockpulse_watch')||'[]')}catch(e){return[]}}, setWatch=a=>localStorage.setItem('stockpulse_watch',JSON.stringify(a));
const getPortfolio=()=>{try{return JSON.parse(localStorage.getItem('stockpulse_portfolio')||'[]')}catch(e){return[]}}, setPortfolio=a=>localStorage.setItem('stockpulse_portfolio',JSON.stringify(a));
function css(){if(document.getElementById('sp4css'))return;const s=document.createElement('style');s.id='sp4css';s.textContent=`
.spSections{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:16px 0}.spTile{padding:14px;border:1px solid #263650;background:#111c2d;color:#eaf0f7;border-radius:12px;text-align:left;cursor:pointer}.spTile b{display:block;font-size:17px}.spTile span{display:block;color:#91a0b4;font-size:11px;margin-top:4px}.spDetail{display:grid;gap:16px}.spHero{display:flex;justify-content:space-between;gap:15px;align-items:flex-start}.spScore{font-size:32px;font-weight:900}.spMetricGrid{display:grid;grid-template-columns:repeat(4,1fr);gap:9px}.spMetric{background:#101a2a;border:1px solid #263650;border-radius:10px;padding:12px}.spMetric b{display:block;font-size:18px}.spMetric span{color:#91a0b4;font-size:11px}.spBlock{border:1px solid #263650;background:#0d1726;border-radius:12px;padding:14px}.spBlock h3{margin:0 0 10px}.spReasons{display:grid;grid-template-columns:1fr 1fr;gap:7px}.spReasons div{padding:9px;border-radius:8px;background:#111c2d}.spTable{width:100%;border-collapse:collapse}.spTable td,.spTable th{padding:8px;border-bottom:1px solid #263650;text-align:left;font-size:12px}.spPositive{color:#45d483}.spNegative{color:#ff6b75}.spPortfolio{display:grid;gap:10px}.spPos{display:grid;grid-template-columns:1.2fr .8fr .8fr auto;gap:12px;align-items:center;padding:13px;border:1px solid #263650;border-radius:11px;background:#101a2a}.spPos small,.spPos span{display:block;color:#91a0b4;font-size:11px}.spKpis{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin:12px 0}.spKpi{padding:14px;border:1px solid #263650;border-radius:11px;background:#101a2a}.spKpi b{display:block;font-size:20px}.spNews a{display:block;color:#eaf0f7;text-decoration:none;padding:10px 0;border-bottom:1px solid #263650}.spNews small{display:block;color:#91a0b4;margin-top:4px}.spActions{display:flex;gap:8px;flex-wrap:wrap}.spActions button{padding:9px 12px;border-radius:8px;border:1px solid #314969;background:#17253a;color:white}.spHint{color:#91a0b4;font-size:11px}.spEmpty{padding:20px;text-align:center;color:#91a0b4}.spRisk{padding:10px;border-left:3px solid #f2b84b;background:#171b20}.spWhyTitle{font-size:13px;color:#91a0b4;margin-bottom:8px}@media(max-width:800px){.spSections,.spMetricGrid,.spReasons,.spKpis{grid-template-columns:repeat(2,1fr)}.spPos{grid-template-columns:1fr 1fr}.spHero{display:block}}`;
document.head.appendChild(s)}
function install(){css();const main=document.querySelector('main');if(!main)return;
if(!document.getElementById('portfolio'))main.insertAdjacentHTML('beforeend',`<section id="portfolio" class="panel hidden"><div class="hero"><div><h2>Personal Portfolio</h2><p>Manual holdings — quantity, buy price, live value and P&amp;L. Stored only on this device.</p></div><button id="spAdd">+ Add holding</button></div><div id="spPort"></div></section>`);
if(!document.getElementById('alerts'))main.insertAdjacentHTML('beforeend',`<section id="alerts" class="panel hidden"><div class="hero"><div><h2>Smart Alerts</h2><p>Rule-based alerts from score, price movement and unusual volume.</p></div></div><div id="spAlerts"></div></section>`);
const add=document.getElementById('spAdd');if(add&&!add.dataset.bound){add.dataset.bound=1;add.onclick=()=>editHolding()};if(window._spInstalled)return;window._spInstalled=1;
const oldSet=window.setTab;window.setTab=function(t){oldSet(t);if(t==='portfolio')renderPortfolio();if(t==='alerts')renderAlerts();if(t==='watch')renderWatch()};
window.openDetail=detail;window.toggleWatch=function(symbol){let a=getWatch();a=a.includes(symbol)?a.filter(x=>x!==symbol):a.concat(symbol);setWatch(a);renderWatch();if(typeof renderAll==='function')renderAll()};window.editHolding=editHolding;window.deleteHolding=deleteHolding;
setTimeout(()=>{installTiles();renderPortfolio();renderAlerts();renderWatch()},300)}
function installTiles(){const hero=document.querySelector('#home .dashHero');if(!hero||document.getElementById('spSections'))return;hero.insertAdjacentHTML('afterend',`<div id="spSections" class="spSections"><button class="spTile" onclick="setTab('screen')"><b>Stock Screener</b><span>Quantitative opportunities</span></button><button class="spTile" onclick="setTab('watch')"><b>Watchlist</b><span>1D → 5Y variations</span></button><button class="spTile" onclick="setTab('portfolio')"><b>Portfolio</b><span>Holdings &amp; P&amp;L</span></button><button class="spTile" onclick="setTab('alerts')"><b>Smart Alerts</b><span>Score &amp; volume signals</span></button></div>`)}
function renderPortfolio(){const box=document.getElementById('spPort');if(!box)return;const hs=getPortfolio();let inv=0,val=0;const rows=hs.map(h=>{const x=(typeof all!=='undefined'?all:[]).find(v=>v.symbol===h.symbol),cur=n(x?.price||h.buyPrice),invested=h.qty*h.buyPrice,value=h.qty*cur,pnl=value-invested;pct;inv+=invested;val+=value;return{h,x,cur,invested,value,pnl,pct:invested?pnl/invested*100:0}}),pnl=val-inv,portPct=inv?pnl/inv*100:0;box.innerHTML=`<div class="spKpis"><div class="spKpi"><b>₹${money(inv)}</b><span>Invested</span></div><div class="spKpi"><b>₹${money(val)}</b><span>Current value</span></div><div class="spKpi"><b class="${pnl>=0?'spPositive':'spNegative'}">${pnl>=0?'+':''}₹${money(pnl)} (${pnl>=0?'+':''}${portPct.toFixed(2)}%)</b><span>Unrealized P&amp;L</span></div></div><div class="spActions"><button onclick="editHolding()">+ Add / Edit holding</button></div><div class="spPortfolio">${rows.length?rows.map(r=>`<div class="spPos"><div><b>${esc(r.h.symbol)}</b><small>${r.h.qty} shares • Buy ₹${money(r.h.buyPrice)}</small></div><div><span>Current</span><b>₹${money(r.cur)}</b></div><div class="${r.pnl>=0?'spPositive':'spNegative'}"><span>P&amp;L</span><b>${r.pnl>=0?'+':''}₹${money(r.pnl)} (${r.pct>=0?'+':''}${r.pct.toFixed(2)}%)</b></div><div><button onclick="openDetail('${esc(r.h.symbol)}')">View</button><button onclick="editHolding('${esc(r.h.symbol)}')">Edit</button><button onclick="deleteHolding('${esc(r.h.symbol)}')">×</button></div></div>`).join(''):'<div class="spEmpty">Portfolio is empty. Add a holding with quantity and buy price.</div>'}</div>`}
function editHolding(symbol){const old=getPortfolio().find(h=>h.symbol===symbol),sym=(symbol||prompt('NSE symbol, e.g. RELIANCE')||'').trim().toUpperCase().replace('.NS','');if(!sym)return;const stock=(typeof all!=='undefined'?all:[]).find(v=>v.symbol===sym);if(!stock){alert('Refresh the screener first so this symbol has current market data.');return}const q=Number(prompt('Quantity',old?.qty||1));if(!q||q<0)return;const buy=Number(prompt('Average buy price',old?.buyPrice||stock.price));if(!buy||buy<0)return;setPortfolio(getPortfolio().filter(h=>h.symbol!==sym).concat({symbol:sym,qty:q,buyPrice:buy,updatedAt:Date.now()}));renderPortfolio()}
function deleteHolding(symbol){if(!confirm('Delete '+symbol+' from portfolio?'))return;setPortfolio(getPortfolio().filter(h=>h.symbol!==symbol));renderPortfolio()}
function renderWatch(){const box=document.getElementById('watchGrid');if(!box)return;const rows=getWatch().map(s=>(typeof all!=='undefined'?all:[]).find(x=>x.symbol===s)).filter(Boolean),period=window._spPeriod||'today',labels={today:'Today','days:1':'1 Day','days:2':'2 Days','days:3':'3 Days','days:5':'5 Days','days:7':'7 Days','days:14':'14 Days','days:30':'30 Days','weeks:1':'1 Week','weeks:2':'2 Weeks','weeks:4':'4 Weeks','weeks:8':'8 Weeks','weeks:12':'12 Weeks','months:1':'1 Month','months:3':'3 Months','months:6':'6 Months','months:12':'1 Year','years:1':'1 Year','years:2':'2 Years','years:3':'3 Years','years:5':'5 Years'},options=Object.entries(labels).map(([k,v])=>`<option value="${k}" ${k===period?'selected':''}>${v}</option>`).join('');box.innerHTML=`<div class="spBlock"><div class="spHero"><div><h3>Watchlist performance</h3><div class="spHint">Compare price variation across multiple periods.</div></div><select id="spPeriod">${options}</select></div></div><div class="grid">${rows.length?rows.map(x=>{let v=n(x.change_pct);if(x.period_returns){const [u,z]=period.split(':');if(x.period_returns[u])v=n(x.period_returns[u][z])}return `<article class="card" onclick="openDetail('${esc(x.symbol)}')"><div class="top"><div><b>${esc(x.symbol)}</b><small>NSE</small></div><strong>${n(x.final_rank_score).toFixed(1)}</strong></div><div class="price">₹${money(x.price)} <span class="${v<0?'down':''}">${pct(v)}</span></div><div class="meta"><span>1D ${pct(x.change_pct)}</span><span>20D ${pct(x.return_20d_pct)}</span><span>1Y ${pct(x.return_1y_pct)}</span></div><button onclick="event.stopPropagation();toggleWatch('${esc(x.symbol)}')">★ Remove</button></article>`}).join(''):'<div class="spEmpty">No stocks in watchlist. Add stocks from Stock Detail.</div>'}</div>`;const p=document.getElementById('spPeriod');if(p)p.onchange=()=>{window._spPeriod=p.value;renderWatch()}}
function renderAlerts(){const box=document.getElementById('spAlerts');if(!box)return;const a=(typeof all!=='undefined'?all:[]).filter(x=>n(x.final_rank_score)>=80||n(x.volume_change_pct)>=50||Math.abs(n(x.change_pct))>=3).sort((x,y)=>n(y.final_rank_score)-n(x.final_rank_score));box.innerHTML=`<div class="spBlock"><h3>Current signals</h3><p class="spHint">Triggered by measurable score, price or volume conditions.</p></div><div class="grid">${a.slice(0,30).map(x=>`<article class="card" onclick="openDetail('${esc(x.symbol)}')"><div class="top"><div><b>${esc(x.symbol)}</b><small>${esc(x.band)}</small></div><strong>${n(x.final_rank_score).toFixed(1)}</strong></div><div class="meta"><span>Today ${pct(x.change_pct)}</span><span>Volume ${pct(x.volume_change_pct)}</span><span>20D ${pct(x.return_20d_pct)}</span></div><ul><li>${n(x.final_rank_score)>=80?'High opportunity score':'Signal detected'}</li>${n(x.volume_change_pct)>=50?'<li>Unusual volume spike</li>':''}${Math.abs(n(x.change_pct))>=3?'<li>Large price movement</li>':''}</ul></article>`).join('')||'<div class="spEmpty">No active alerts.</div>'}</div>`}
async function detail(symbol){const modal=document.getElementById('detail'),body=document.getElementById('detailBody');if(!modal||!body)return;body.innerHTML='<div class="spEmpty">Loading full stock analysis…</div>';modal.classList.remove('hidden');try{const r=await fetch(API+'/stock/'+encodeURIComponent(symbol)+'?ts='+Date.now());if(!r.ok)throw Error('API '+r.status);const x=await r.json(),fav=getWatch().includes(x.symbol),reasons=x.why||[],pros=x.pros||[],cons=x.cons||[];body.innerHTML=`<div class="spDetail"><div class="spHero"><div><small>NSE • ${esc(x.sector||'Indian Equity')}</small><h2>${esc(x.company_name||x.symbol)} (${esc(x.symbol)})</h2><div class="detailPrice">₹${money(x.price)} <span class="${n(x.change_pct)<0?'down':''}">${pct(x.change_pct)}</span></div><div class="spHint">${esc(x.industry||'')}</div></div><div><div class="spScore">${n(x.final_rank_score).toFixed(1)}<small>/100</small></div><b>${esc(x.recommendation)} • ${esc(x.risk)} Risk</b></div></div><div class="spActions"><button onclick="toggleWatch('${esc(x.symbol)}');openDetail('${esc(x.symbol)}')">${fav?'★ Remove from Watchlist':'☆ Add to Watchlist'}</button><button onclick="editHolding('${esc(x.symbol)}')">+ Add to Portfolio</button></div><div class="spMetricGrid"><div class="spMetric"><span>Estimated upside</span><b>${pct(x.estimated_upside_pct)}</b></div><div class="spMetric"><span>20D return</span><b>${pct(x.return_20d_pct)}</b></div><div class="spMetric"><span>60D return</span><b>${pct(x.return_60d_pct)}</b></div><div class="spMetric"><span>52W position</span><b>${n(x['52w_position']).toFixed(0)}%</b></div></div><div class="spBlock"><h3>Why StockPulse recommends this stock</h3><div class="spWhyTitle">Reasons are tied to measurable market and fundamental fields.</div><div class="spReasons">${reasons.map(v=>`<div>✓ ${esc(v)}</div>`).join('')||'<div>No positive trigger currently recorded.</div>'}</div></div><div class="spBlock"><h3>Score breakdown</h3><table class="spTable"><tr><th>Factor</th><th>Weight</th><th>Evidence</th></tr><tr><td>Fundamental</td><td>35%</td><td>ROE ${x.roe==null?'—':(n(x.roe)*100).toFixed(1)+'%'} • Margin ${x.profit_margin==null?'—':(n(x.profit_margin)*100).toFixed(1)+'%'}</td></tr><tr><td>Valuation</td><td>20%</td><td>PE ${x.pe==null?'—':n(x.pe).toFixed(1)} • PB ${x.pb==null?'—':n(x.pb).toFixed(1)}</td></tr><tr><td>Technical / Momentum</td><td>20%</td><td>RSI ${n(x.rsi14).toFixed(1)} • DMA20 ₹${money(x.dma20)} • DMA50 ₹${money(x.dma50)}</td></tr><tr><td>Catalyst</td><td>15%</td><td>Trend + volume confirmation</td></tr><tr><td>Risk</td><td>10%</td><td>D/E ${x.debt_to_equity==null?'—':n(x.debt_to_equity).toFixed(0)+'%'}</td></tr></table></div><div class="spBlock"><h3>Volume & price action</h3><table class="spTable"><tr><td>Today volume</td><td>${money(x.volume_today)}</td></tr><tr><td>5D average</td><td>${money(x.volume_5d_avg)}</td></tr><tr><td>20D average</td><td>${money(x.volume_20d_avg)}</td></tr><tr><td>Volume vs 20D average</td><td>${pct(x.volume_change_pct)}</td></tr><tr><td>RSI</td><td>${n(x.rsi14).toFixed(1)}</td></tr><tr><td>DMA 20 / 50 / 200</td><td>₹${money(x.dma20)} / ₹${money(x.dma50)} / ₹${money(x.dma200)}</td></tr></table></div><div class="spBlock"><h3>Company snapshot</h3><p>${esc(x.business_summary||'Fundamental company description is not available from the current public data feed.')}</p><table class="spTable"><tr><td>Market cap</td><td>${x.market_cap?'₹'+money(x.market_cap):'—'}</td></tr><tr><td>52W high / low</td><td>₹${money(x['52w_high'])} / ₹${money(x['52w_low'])}</td></tr><tr><td>PE / PB</td><td>${x.pe==null?'—':n(x.pe).toFixed(1)} / ${x.pb==null?'—':n(x.pb).toFixed(1)}</td></tr><tr><td>ROE</td><td>${x.roe==null?'—':(n(x.roe)*100).toFixed(1)+'%'}</td></tr><tr><td>Debt/Equity</td><td>${x.debt_to_equity==null?'—':n(x.debt_to_equity).toFixed(1)+'%'}</td></tr></table></div><div class="spBlock"><h3>Pros & Cons</h3><div class="spReasons"><div><b>Pros</b><br>${pros.map(esc).join('<br>')||'No extra quantitative positive flag.'}</div><div class="spRisk"><b>Risks / Cons</b><br>${cons.map(esc).join('<br>')}</div></div></div><div class="spBlock spNews" id="spNews"><h3>Latest news / catalysts</h3><div class="spHint">Loading public news feed…</div></div><div class="spBlock"><h3>Recommendation by horizon</h3><table class="spTable"><tr><th>Horizon</th><th>Signal</th><th>Score</th><th>Estimated upside</th></tr><tr><td>Short term</td><td>${x.short_term_score>=70?'BUY':'WATCH'}</td><td>${n(x.short_term_score).toFixed(1)}</td><td>${pct(Math.max(0,n(x.estimated_upside_pct)*.45))}</td></tr><tr><td>Medium term</td><td>${x.medium_term_score>=70?'BUY':'WATCH'}</td><td>${n(x.medium_term_score).toFixed(1)}</td><td>${pct(Math.max(0,n(x.estimated_upside_pct)*.75))}</td></tr><tr><td>Long term</td><td>${x.long_term_score>=70?'BUY':'WATCH'}</td><td>${n(x.long_term_score).toFixed(1)}</td><td>${pct(Math.max(0,n(x.estimated_upside_pct)))}</td></tr></table><div class="spHint">Estimated upside is a model estimate, not a guaranteed return or investment advice.</div></div></div>`;loadNews(x.symbol)}catch(e){body.innerHTML='<div class="spEmpty">Unable to load full stock analysis right now. Please refresh and try again.</div>'}}
async function loadNews(symbol){const box=document.getElementById('spNews');if(!box)return;try{const rss='https://news.google.com/rss/search?hl=en-IN&gl=IN&ceid=IN:en&q='+encodeURIComponent(symbol+' India stock results order announcement'),r=await fetch('https://api.rss2json.com/v1/api.json?rss_url='+encodeURIComponent(rss)),d=await r.json(),items=(d.items||[]).slice(0,8);box.innerHTML='<h3>Latest news / catalysts</h3>'+(items.length?items.map(i=>`<a href="${esc(i.link)}" target="_blank" rel="noopener"><b>${esc(i.title)}</b><small>${new Date(i.pubDate).toLocaleString('en-IN')}</small></a>`).join(''):'<div class="spHint">No public headlines available.</div>')}catch(e){box.innerHTML='<h3>Latest news / catalysts</h3><div class="spHint">News feed temporarily unavailable.</div>'}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
})();
